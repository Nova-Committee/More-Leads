{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "sources": "https://github.com/Nova-Committee/more-leads"
  },
  "license": "${license}",
  "icon": "${mod_id}.png",
  "environment": "server",
  "entrypoints": {
    "main": [
      "committee.nova.mods.moreleads.MoreLeadsFabric"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json"
  ],
  "depends": {
    "fabric-api": "*",
    "minecraft": "${minecraft_version_range}"
  }
}
